import styled from "styled-components"
import Box from "../../../../../elements/Box"

const VideoControlsWrapper = styled(Box).attrs(props =>({
className: 'Video-Controls-Wrapper'
}))`

z-index: 1;
text-align: center;


`

export default VideoControlsWrapper
import styled from "styled-components"
import Box from "../../../elements/Box"


const PostSummary = styled(Box).attrs(props => ({
    className: 'card-text',
    as: 'p'
}))``

export default PostSummary
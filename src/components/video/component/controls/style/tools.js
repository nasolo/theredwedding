import styled from "styled-components"
import Box from "../../../../../elements/Box"

const ToolsControls = styled(Box).attrs(props =>({
    className: "tools-controls"
}))`

    flex: 1 1;
    overflow: hidden;
    display: flex;
    justify-content: flex-start;
    
    


`

export default ToolsControls
import styled from "styled-components"
import Box from "../../../../../elements/Box"

const VolumeControls = styled(Box).attrs(props => ({
className: `volume-controls`
}))`

    vertical-align: center;
    margin: auto;
    height: 100%;

    
`


export default VolumeControls